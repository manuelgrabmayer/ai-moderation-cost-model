#!/usr/bin/env python3
from pathlib import Path
from shutil import copy2


ROOT = Path(__file__).resolve().parents[1]
OUT_PATH = ROOT / 'output' / 'files' / 'figure_generation_sources' / 'fn' / 'costs_fn_1.csv'
LEGACY_INPUT = ROOT / 'input' / 'figure_generation_sources' / 'fn' / 'costs_fn_1.csv'

# Upstream generator provenance: FN/False_negative_costs.py
UPSTREAM_PATHS = [
    ROOT.parents[1] / 'Coding - cost parameters ' / 'Cost parameters' / 'outputs' / 'FN_Cost_Parameters' / 'costs_fn_1.csv',
    ROOT.parents[1] / 'Coding - cost parameters ' / 'Cost parameters' / 'outputs' / 'FN_cost_parameters' / 'costs_fn_1.csv',
]


def main() -> int:
    OUT_PATH.parent.mkdir(parents=True, exist_ok=True)
    if OUT_PATH.exists():
        print(f'Exists: {OUT_PATH}')
        return 0

    for src in [LEGACY_INPUT, *UPSTREAM_PATHS]:
        if src.exists():
            copy2(src, OUT_PATH)
            print(f'Copied: {src}')
            print(f'Wrote: {OUT_PATH}')
            return 0

    raise FileNotFoundError(
        'Could not locate costs_fn_1.csv. Expected legacy input or upstream outputs '
        '(generated by FN/False_negative_costs.py).'
    )


if __name__ == '__main__':
    raise SystemExit(main())
